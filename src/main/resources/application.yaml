# ─────────────────────────────────────────────────────────────────────────────
# application.yaml
# Sensitive values are loaded from environment variables so that no secrets
# are committed to source control.  See .env.example in the project root.
# ─────────────────────────────────────────────────────────────────────────────
spring:
  application:
    name: Banking-System
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://choudharykhushboogirdhariram_db_user:Gmc9035*@cluster0.hixs00o.mongodb.net/?appName=Cluster0}
      database: banking_db
      auto-index-creation: true
server:
  port: 8080
jwt:
  secret: ${JWT_SECRET:234567890ytrdcvbvcxsw23456789iok1234567890qwertyuioasdfghjkzxcvbnm}
  expiration: 86400000

cors:
  allowed-origins: ${ALLOWED_ORIGIN:https://banking-system-ashy-pi.vercel.app}

logging:
  level:
    org.springframework.security: INFO
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
